class Blogs {
  String image, uid, authorname, title, description, likes, date, time;
  int timeStamp;
  Blogs(this.image, this.uid, this.authorname, this.title, this.description,
      this.date, this.likes, this.time, this.timeStamp)
      : super();
}
